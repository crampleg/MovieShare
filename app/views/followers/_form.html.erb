<%= form_for(@follower) do |f| %>
  <% if @follower.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@follower.errors.count, "error") %> prohibited this follower from being saved:</h2>

      <ul>
      <% @follower.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id_model %><br />
    <%= f.number_field :user_id_model %>
  </div>
  <div class="field">
    <%= f.label :user_id_follower %><br />
    <%= f.number_field :user_id_follower %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
