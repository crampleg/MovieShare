<%= form_for(@my_list) do |f| %>
  <% if @my_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_list.errors.count, "error") %> prohibited this my_list from being saved:</h2>

      <ul>
      <% @my_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
   
    
  </div>
  <div class="field">
    <%= f.label :list_name %><br />
    <%= f.text_field :list_name %>
  </div>
  <div class="field">
  
   
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
